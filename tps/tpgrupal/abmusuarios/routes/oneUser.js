/* 
var express = require('express');
var router = express.Router();
var dbu; 

//router.get('/getUsers', function(req, res, next) {
router.get('/users', function(req, res, next) {
	db.createUser
  	res.json('respond with a resource');
}); 



module.exports = function(dbApp, appApp){ 
	 dbu = dbApp; 
     appApp.use('/users', router);
};
*/
//aca va a haber todas rutas de get y devuelven el renderizado de un template
//las rutas de api devuelven json y no tienen views y tienen los verbos de http put, 
//delete, get, post 

